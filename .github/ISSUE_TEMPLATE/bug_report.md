---
name: Bug report
about: Something rendered wrong, or the script errored out
title: ""
labels: bug
---

**What happened**

**What you expected**

**Environment**
- OS (Linux/WSL/macOS/Windows — note: macOS and native Windows are not
  yet verified, see CLAUDE.md):
- `bash --version`:
- `jq --version`:
- Install path (plugin marketplace / curl install.sh):
- Layout (`minimal`/`hardened`/`power`) and any custom `segments`/`separator`:

**Config**

Paste your `~/.claude/statusline-fleetline.config.json` if you have one
(redact anything you don't want public — it shouldn't contain secrets,
but double-check).

```json

```

**Raw payload (if you can capture it)**

The exact stdin JSON that produced the wrong output helps the most —
redact anything sensitive (paths, session content).

```json

```
